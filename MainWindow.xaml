<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Ra2Helper.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ra2Helper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Ra2Helper">

    <Grid RowDefinitions ="Auto, Auto, *">
        <InfoBar x:Name="Notice" IsOpen="True" IsClosable="False" Severity="Informational"
                 Message="Commander, where is our Red Alert 2 command center?" Grid.Row="0" />
        <Grid x:Name="Prepare" Grid.Row="1" Margin="20">
            <Button Click="SelectGame_Click" Content="Select game.exe" HorizontalAlignment="Left"
                Background="#0078d4" Foreground="White" />
            <Button Click="FixPermission_Click" Content="Fix permission" HorizontalAlignment="Right"
     Background="#c42b1c" Foreground="White" x:Name="FixPermission" Visibility="Collapsed" />
        </Grid>
        <Grid x:Name="Features" Grid.Row="2" RowDefinitions="Auto, Auto, Auto" Margin="30,0">
            <ComboBox x:Name="Resolutions" Header="Resolution" PlaceholderText="Select a resolution"
                      Width="200" SelectionChanged="Resolutions_SelectionChanged"  Grid.Row="0" IsEnabled="False" />
            <TextBlock Text="Fix" Grid.Row="1" Margin="0,10"/>
            <ToggleButton x:Name="FixLanBattle" Content="LAN Battle" Click="FixLanBattle_Click" Grid.Row="2" IsEnabled="False" />
        </Grid>
    </Grid>
</Window>
